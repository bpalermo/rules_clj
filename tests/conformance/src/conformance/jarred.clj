(ns conformance.jarred
  "Stands in for a Clojure library published to Maven: this file is packaged into a
  jar as SOURCE, the way Clojars publishes, and never compiled by the target that
  ships it.")

(defn value [] :jarred)
