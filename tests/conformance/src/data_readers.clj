;; A reader tag has to be visible on the compile classpath, because it is consulted
;; while READING the source of a namespace being compiled — before any of it runs.
{conformance/upper conformance.readers/read-upper}
