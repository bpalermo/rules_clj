# Generated by `bazel run //tools/tidy`. Edit the ns forms, not this file.
load("@rules_clj//clojure:defs.bzl", "clj_library", "clj_test")

package(default_visibility = ["//visibility:public"])

clj_library(
    name = "store_cart",
    srcs = ["cart.clj"],
    namespaces = ["store.cart"],
    strip_prefix = "src",
    deps = [
        "//src/store:store_item",
    ],
)

clj_library(
    name = "store_cart_test",
    testonly = True,
    srcs = ["cart_test.clj"],
    namespaces = ["store.cart-test"],
    strip_prefix = "src",
    deps = [
        "//src/store:store_cart",
    ],
)

clj_library(
    name = "store_item",
    srcs = ["item.clj"],
    namespaces = ["store.item"],
    strip_prefix = "src",
)

clj_test(
    name = "store_cart_test.test",
    ns = "store.cart-test",
    deps = [":store_cart_test"],
)
